<!DOCTYPE html>
<html>
<head>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
</head>
<body>
    <header>
        <div class="wrapper">
            <%= image_tag('logo.png')%>
            <span>Twitter Clone</span>
            <% if current_user %>
                <%= link_to "Buddies' Ribbits", buddies_path %>
                <%= link_to "Public Ribbits", ribbits_path %>
                <%= link_to "Public User",users_path%>
                <%= link_to "My Profile", current_user%>
                <%= link_to "Log Out", sessions_destroy_path %>
            <% else %>
                <%= form_tag sessions_create_path do %>
                    <%= text_field_tag :username, nil, placeholder: "username" %>
                    <%= password_field_tag :password, nil,  placeholder: "password" %>
                    <%= submit_tag "Log In" %>
                <% end %>
            <% end %>
        </div>
    </header>
    <div id="content">
        <div class="wrapper">
            <% flash.each do |name, msg| %>
                <%= content_tag :div, msg, class: "flash #{name}" %>
            <% end %>
            <%= yield %>
        </div>
    </div>
    <footer>
        <div class="wrapper">
            Ribbit - A Twitter Clone Tutorial<%= image_tag('logo-nettuts.png')%>
        </div>
    </footer>
</body>
</html>